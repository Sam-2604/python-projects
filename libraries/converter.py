# Creates a separate file called converter.py containing two functions — usd_to_inr(amount) and inr_to_usd(amount)
# — use a hardcoded rate of 83.5.


def usd_to_inr(amount):
    return amount * 83.5


def inr_to_usd(amount):
    return amount / 83.5
